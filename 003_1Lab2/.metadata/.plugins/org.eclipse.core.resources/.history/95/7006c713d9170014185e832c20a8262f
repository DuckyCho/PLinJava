package sorting;

public class Main {

	public static void main(String[] args) {
		NumberSet numset = new NumberSet("I");
		
		
		System.out.print("Before Sort : ");
		new Print(numset);
		Sort.executeSort(numset);
		System.out.print("After Sort : ");
		new Print(numset);
		
	}

}
